commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit

pre-commit:
  parallel: true
  commands:
    eslint:
      glob: "*.{js,ts,jsx,tsx}"
      exclude: "docs|dist"
      run: npx eslint {staged_files}
    prettify:
      glob: "*.{js,ts,jsx,tsx}"
      exclude: "docs|dist"
      run: npx prettier --write {staged_files}

pre-push:
  commands:
    test:
      run: npx jest